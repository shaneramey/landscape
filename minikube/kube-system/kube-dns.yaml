name: kube-dns
namespace: kube-system
release:
  chart: chartmuseum/kube-dns
  version: 0.1.31
configuration:
  clusterDomain: cluster.local
  services:
    dns:
      type: ClusterIP
      clusterIP: 10.0.0.10
