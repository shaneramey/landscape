name: jenkins
release:
  chart: charts.downup.us/jenkins:0.6.209
  version: 0.6.209
configuration:
  clusterDomain: master.local
secrets:
 - jenkins-admin-user
 - jenkins-admin-password
 - jenkins-github-access-token
 - jenkins-vault-user
 - jenkins-vault-password
