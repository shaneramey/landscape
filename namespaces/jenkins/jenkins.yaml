name: jenkins
release:
  chart: charts.downup.us/jenkins:0.6.177
  version: 0.6.177
configuration:
  clusterDomain: master.local
secrets:
 - jenkins-admin-user
 - jenkins-admin-password
 - jenkins-github-access-token
